# KPI Card Viz Extension — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans. Steps use `- [ ]` syntax. Update to `- [x]` as each step completes.

**Goal:** Build and deploy a Tableau Viz Extension that renders a clean KPI card component with four display modes (Simple, Comparison, Trend, Sparkline) using D3.js v7 and the Tableau Extensions API 2.x, published to GitHub Pages.

**Architecture:** A pure HTML/JS extension (no build step) served from GitHub Pages, structured identically to the existing `multi-line-chart` extension. The `.trex` manifest declares five encoding slots (`value`, `label`, `comparison`, `history`, `date`); `chart.js` reads live data via the Extensions API, determines which mode to render, and draws the card; a `test.html` page mocks the entire Tableau API so the card can be verified in any browser without Tableau Desktop.

**Tech Stack:** D3.js v7, Tableau Extensions API 2.x, vanilla JS, GitHub Pages

---

## Progress Tracker

- [x] Task 1 — TREX manifests (production + local)
- [x] Task 2 — `index.html` (card CSS + mode button styles)
- [x] Task 3 — `chart.js` (all 4 card modes, full rendering logic)
- [x] Task 4 — `test.html` (mock with realistic revenue KPI data)
- [ ] Task 5 — `README.md`
- [ ] Task 6 — Push all

---

## File Map

| Path | Role |
|---|---|
| `extensions/kpi-card/kpi-card.trex` | Production manifest (GitHub Pages URL) |
| `extensions/kpi-card/kpi-card-local.trex` | Local dev manifest (localhost:8080) |
| `extensions/kpi-card/index.html` | Extension shell loaded by Tableau in an iframe |
| `extensions/kpi-card/chart.js` | All rendering logic + Tableau API integration |
| `extensions/kpi-card/test.html` | Standalone browser test — mocks Tableau API with revenue KPI data |
| `extensions/kpi-card/README.md` | Usage, encoding slots, dev workflow |

---

## Task 1: TREX Manifests

**Files:**
- Create: `extensions/kpi-card/kpi-card.trex`
- Create: `extensions/kpi-card/kpi-card-local.trex`

- [x] **Step 1.1: Create production manifest**

Create `extensions/kpi-card/kpi-card.trex`:

```xml
<?xml version="1.0" encoding="utf-8"?>
<manifest manifest-version="0.1" xmlns="http://www.tableau.com/xml/extension_manifest">
  <worksheet-extension id="com.cyntexa.kpicard" extension-version="1.0.0">
    <default-locale>en_US</default-locale>
    <name resource-id="name"/>
    <description>KPI card with Simple, Comparison, Trend, and Sparkline display modes</description>
    <author name="Cyntexa" email="vishwajeet@cyntexa.com" organization="Cyntexa" website="https://cyntexa.com"/>
    <min-api-version>1.1</min-api-version>
    <source-location>
      <url>https://vj-cyntexa.github.io/tableau-viz/extensions/kpi-card/index.html</url>
    </source-location>
    <icon/>
    <encoding id="value">
      <display-name resource-id="value_label">Value (Primary Measure)</display-name>
      <role-spec><role-type>continuous-measure</role-type></role-spec>
      <fields min-count="1" max-count="1"/>
    </encoding>
    <encoding id="label">
      <display-name resource-id="label_label">Label (Card Title)</display-name>
      <role-spec><role-type>discrete-dimension</role-type></role-spec>
      <fields min-count="0" max-count="1"/>
    </encoding>
    <encoding id="comparison">
      <display-name resource-id="comparison_label">Comparison (Secondary Measure)</display-name>
      <role-spec><role-type>continuous-measure</role-type></role-spec>
      <fields min-count="0" max-count="1"/>
    </encoding>
    <encoding id="history">
      <display-name resource-id="history_label">History (Sparkline Measure)</display-name>
      <role-spec><role-type>continuous-measure</role-type></role-spec>
      <fields min-count="0" max-count="1"/>
    </encoding>
    <encoding id="date">
      <display-name resource-id="date_label">Date (Sparkline X Axis)</display-name>
      <role-spec><role-type>discrete-dimension</role-type></role-spec>
      <fields min-count="0" max-count="1"/>
    </encoding>
  </worksheet-extension>
  <resources>
    <resource id="name"><text locale="en_US">KPI Card</text></resource>
    <resource id="value_label"><text locale="en_US">Value (Primary Measure)</text></resource>
    <resource id="label_label"><text locale="en_US">Label (Card Title)</text></resource>
    <resource id="comparison_label"><text locale="en_US">Comparison (Secondary Measure)</text></resource>
    <resource id="history_label"><text locale="en_US">History (Sparkline Measure)</text></resource>
    <resource id="date_label"><text locale="en_US">Date (Sparkline X Axis)</text></resource>
  </resources>
</manifest>
```

- [x] **Step 1.2: Create local dev manifest**

Create `extensions/kpi-card/kpi-card-local.trex` — identical to above except `id` and `<url>`:

```xml
<?xml version="1.0" encoding="utf-8"?>
<manifest manifest-version="0.1" xmlns="http://www.tableau.com/xml/extension_manifest">
  <worksheet-extension id="com.cyntexa.kpicard.local" extension-version="1.0.0">
    <default-locale>en_US</default-locale>
    <name resource-id="name"/>
    <description>KPI card with Simple, Comparison, Trend, and Sparkline display modes (local dev)</description>
    <author name="Cyntexa" email="vishwajeet@cyntexa.com" organization="Cyntexa" website="https://cyntexa.com"/>
    <min-api-version>1.1</min-api-version>
    <source-location>
      <url>http://localhost:8080/extensions/kpi-card/index.html</url>
    </source-location>
    <icon/>
    <encoding id="value">
      <display-name resource-id="value_label">Value (Primary Measure)</display-name>
      <role-spec><role-type>continuous-measure</role-type></role-spec>
      <fields min-count="1" max-count="1"/>
    </encoding>
    <encoding id="label">
      <display-name resource-id="label_label">Label (Card Title)</display-name>
      <role-spec><role-type>discrete-dimension</role-type></role-spec>
      <fields min-count="0" max-count="1"/>
    </encoding>
    <encoding id="comparison">
      <display-name resource-id="comparison_label">Comparison (Secondary Measure)</display-name>
      <role-spec><role-type>continuous-measure</role-type></role-spec>
      <fields min-count="0" max-count="1"/>
    </encoding>
    <encoding id="history">
      <display-name resource-id="history_label">History (Sparkline Measure)</display-name>
      <role-spec><role-type>continuous-measure</role-type></role-spec>
      <fields min-count="0" max-count="1"/>
    </encoding>
    <encoding id="date">
      <display-name resource-id="date_label">Date (Sparkline X Axis)</display-name>
      <role-spec><role-type>discrete-dimension</role-type></role-spec>
      <fields min-count="0" max-count="1"/>
    </encoding>
  </worksheet-extension>
  <resources>
    <resource id="name"><text locale="en_US">KPI Card (local)</text></resource>
    <resource id="value_label"><text locale="en_US">Value (Primary Measure)</text></resource>
    <resource id="label_label"><text locale="en_US">Label (Card Title)</text></resource>
    <resource id="comparison_label"><text locale="en_US">Comparison (Secondary Measure)</text></resource>
    <resource id="history_label"><text locale="en_US">History (Sparkline Measure)</text></resource>
    <resource id="date_label"><text locale="en_US">Date (Sparkline X Axis)</text></resource>
  </resources>
</manifest>
```

- [x] **Step 1.3: Commit**

```bash
git add extensions/kpi-card/
git commit -m "feat: add .trex manifests for kpi-card viz extension"
```

---

## Task 2: HTML Shell (`index.html`)

**Files:**
- Create: `extensions/kpi-card/index.html`

- [x] **Step 2.1: Write index.html**

Create `extensions/kpi-card/index.html`:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>KPI Card</title>
  <script src="https://cdn.jsdelivr.net/npm/d3@7"></script>
  <script src="https://extensions.tableau.com/lib/tableau.extensions.2.latest.js"></script>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica, Arial, sans-serif;
      background: #f7f8fa;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      overflow: hidden;
    }

    /* ── Card container ─────────────────────────────────────────────────── */
    #kpi-card {
      position: relative;
      background: #ffffff;
      border-radius: 12px;
      box-shadow: 0 2px 12px rgba(0, 0, 0, 0.10);
      padding: 32px 36px 28px;
      min-width: 240px;
      max-width: 420px;
      width: 90vw;
      text-align: center;
    }

    /* ── Mode pill buttons ──────────────────────────────────────────────── */
    #mode-buttons {
      position: absolute;
      top: 12px;
      right: 12px;
      display: flex;
      gap: 4px;
    }

    .mode-btn {
      font-size: 10px;
      font-weight: 600;
      letter-spacing: 0.03em;
      padding: 3px 8px;
      border-radius: 20px;
      border: 1.5px solid #c9cdd4;
      background: transparent;
      color: #7a828e;
      cursor: pointer;
      transition: background 0.15s, color 0.15s, border-color 0.15s;
      line-height: 1.5;
    }

    .mode-btn:hover {
      border-color: #3498db;
      color: #3498db;
    }

    .mode-btn.active {
      background: #3498db;
      border-color: #3498db;
      color: #ffffff;
    }

    /* ── Card label / title ─────────────────────────────────────────────── */
    .kpi-label {
      font-size: 12px;
      font-weight: 600;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: #9198a1;
      margin-bottom: 10px;
    }

    /* ── Primary value ──────────────────────────────────────────────────── */
    .kpi-value {
      font-size: 48px;
      font-weight: 700;
      line-height: 1.1;
      color: #1a2130;
      letter-spacing: -0.02em;
    }

    /* ── Delta / comparison row ─────────────────────────────────────────── */
    .kpi-comparison-row {
      margin-top: 10px;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      font-size: 14px;
      color: #555e6a;
    }

    .kpi-prev-value {
      font-weight: 500;
    }

    .kpi-delta {
      font-weight: 700;
      font-size: 15px;
    }

    .kpi-delta.positive { color: #27ae60; }
    .kpi-delta.negative { color: #e74c3c; }
    .kpi-delta.neutral  { color: #3498db; }

    /* ── Trend background card ──────────────────────────────────────────── */
    #kpi-card.trend-up {
      background: linear-gradient(135deg, #eafaf1 0%, #d5f5e3 100%);
    }
    #kpi-card.trend-down {
      background: linear-gradient(135deg, #fef9f9 0%, #fde8e8 100%);
    }

    .kpi-trend-arrow {
      font-size: 28px;
      margin-top: 6px;
    }

    .kpi-trend-arrow.up   { color: #27ae60; }
    .kpi-trend-arrow.down { color: #e74c3c; }

    /* ── Sparkline ──────────────────────────────────────────────────────── */
    .kpi-sparkline {
      margin-top: 16px;
      display: flex;
      justify-content: center;
    }

    .kpi-sparkline svg {
      display: block;
    }

    /* ── Hint text (shown when optional encoding is missing) ────────────── */
    .kpi-hint {
      margin-top: 10px;
      font-size: 11px;
      color: #b0b8c1;
      font-style: italic;
    }

    /* ── Error banner ───────────────────────────────────────────────────── */
    #error {
      display: none;
      position: fixed;
      bottom: 12px;
      left: 50%;
      transform: translateX(-50%);
      background: #fff0f0;
      color: #c0392b;
      border: 1px solid #f5c6c6;
      border-radius: 6px;
      padding: 8px 16px;
      font-size: 12px;
      z-index: 200;
      max-width: 80vw;
      text-align: center;
    }
    #error:not(:empty) { display: block; }
  </style>
</head>
<body>
  <div id="kpi-card">
    <div id="mode-buttons">
      <button class="mode-btn active" data-mode="simple">Simple</button>
      <button class="mode-btn" data-mode="comparison">Comparison</button>
      <button class="mode-btn" data-mode="trend">Trend</button>
      <button class="mode-btn" data-mode="sparkline">Sparkline</button>
    </div>
    <div id="card-body"></div>
  </div>
  <div id="error"></div>
  <script src="chart.js"></script>
</body>
</html>
```

- [x] **Step 2.2: Verify the file is valid HTML**

Open `extensions/kpi-card/index.html` in a browser. You should see a white rounded card with four pill buttons (Simple, Comparison, Trend, Sparkline) in the top-right corner. The card body will be empty (the Tableau Extensions API will throw a benign init error — expected outside Tableau).

- [x] **Step 2.3: Commit**

```bash
git add extensions/kpi-card/index.html
git commit -m "feat: add html shell and CSS for kpi-card extension"
```

---

## Task 3: Chart Logic (`chart.js`)

**Files:**
- Create: `extensions/kpi-card/chart.js`

**Data-shape contract:**
- Tableau delivers one row per date (when `date` is on the view) or one aggregate row otherwise.
- `value` and `comparison` are read from whichever row has the **largest date** value (i.e. the most recent period). If there is only one aggregate row, that row is used directly.
- `history` values across all rows become the sparkline series, sorted ascending by `date`.
- Delta %: `(value - comparison) / Math.abs(comparison)`. Guard: if `comparison === 0`, show `N/A`.
- Selected mode is stored in the module-scope variable `currentMode` so it survives `SummaryDataChanged` re-renders.
- Default mode on first load: `simple`.
- Graceful fallback: if user picks a mode whose required optional encoding is absent, render Simple with an inline hint.

- [ ] **Step 3.1: Write chart.js**

Create `extensions/kpi-card/chart.js`:

```javascript
'use strict';

// ─── Colors ──────────────────────────────────────────────────────────────────
const COLOR_SUCCESS = '#27ae60';
const COLOR_DANGER  = '#e74c3c';
const COLOR_NEUTRAL = '#3498db';

// ─── Format helpers ───────────────────────────────────────────────────────────
// d3.format(',.2s') produces e.g. "1.2M", "340k" — used as-is per spec.
const fmtBig   = d3.format(',.2s');
const fmtDelta = d3.format('+.1%');

// ─── Mode state (module-scope so it survives re-renders) ──────────────────────
let currentMode = 'simple';

// ─── Bootstrap ───────────────────────────────────────────────────────────────
document.querySelectorAll('.mode-btn').forEach(btn => {
  btn.addEventListener('click', () => {
    currentMode = btn.dataset.mode;
    document.querySelectorAll('.mode-btn').forEach(b => b.classList.remove('active'));
    btn.classList.add('active');
    // Re-render with the cached data if available.
    if (window._lastKpiData) renderCard(window._lastKpiData, currentMode);
  });
});

tableau.extensions.initializeAsync().then(() => {
  const ws = tableau.extensions.worksheetContent.worksheet;
  ws.addEventListener(tableau.TableauEventType.SummaryDataChanged, () => render(ws));
  render(ws);
}).catch(err => {
  setError(err.message || String(err));
});

// ─── Render pipeline ─────────────────────────────────────────────────────────
async function render(worksheet) {
  clearError();
  try {
    const [vizSpec, dataTable] = await Promise.all([
      worksheet.getVisualSpecificationAsync(),
      fetchData(worksheet),
    ]);
    const kpiData = parseTableauData(dataTable, vizSpec);
    window._lastKpiData = kpiData;   // cache for mode-button re-renders
    renderCard(kpiData, currentMode);
  } catch (err) {
    setError(err.message || String(err));
  }
}

async function fetchData(worksheet) {
  const reader = await worksheet.getSummaryDataReaderAsync(undefined, {
    ignoreSelection: true,
  });
  const table = await reader.getAllPagesAsync();
  await reader.releaseAsync();
  return table;
}

// ─── Data parsing ─────────────────────────────────────────────────────────────
// Returns a plain object:
// {
//   value:      number,           // primary KPI value (from most-recent row)
//   valueField: string,           // field name for fallback label
//   label:      string | null,    // from label encoding or null
//   comparison: number | null,    // from comparison encoding or null
//   history:    Array<{ date: Date|null, val: number }>,  // sparkline points
//   hasDate:    boolean,
//   hasComparison: boolean,
//   hasHistory:    boolean,
// }
function parseTableauData(dataTable, vizSpec) {
  const marksSpec = vizSpec.marksSpecificationCollection[0];
  if (!marksSpec) throw new Error('No marks specification found.');

  const encodings = marksSpec.encodingCollection;

  function fieldName(encodingId) {
    const enc = encodings.find(e => e.id === encodingId);
    return enc?.fieldCollection?.[0]?.fieldName ?? null;
  }

  const valueField      = fieldName('value');
  const labelField      = fieldName('label');
  const comparisonField = fieldName('comparison');
  const historyField    = fieldName('history');
  const dateField       = fieldName('date');

  // Build column index by field name.
  const colIndex = Object.fromEntries(
    dataTable.columns.map(c => [c.fieldName, c.index])
  );

  if (!valueField || !(valueField in colIndex)) {
    throw new Error('Drag a numeric measure onto the "Value (Primary Measure)" encoding slot.');
  }

  const vi  = colIndex[valueField];
  const li  = labelField      && labelField      in colIndex ? colIndex[labelField]      : null;
  const ci  = comparisonField && comparisonField in colIndex ? colIndex[comparisonField] : null;
  const hi  = historyField    && historyField    in colIndex ? colIndex[historyField]    : null;
  const di  = dateField       && dateField       in colIndex ? colIndex[dateField]       : null;

  const parseDate = v => {
    if (!v) return null;
    const d = new Date(v);
    return isNaN(d.getTime()) ? null : d;
  };

  // Parse all rows.
  const rows = dataTable.data.map(row => ({
    value:      Number(row[vi].value),
    label:      li !== null ? String(row[li].value) : null,
    comparison: ci !== null ? Number(row[ci].value) : null,
    date:       di !== null ? parseDate(row[di].value) : null,
    history:    hi !== null ? Number(row[hi].value)    : null,
  }));

  if (!rows.length) throw new Error('No data rows found. Check that the encoding slots have valid fields.');

  // Determine the "current" row: last by date if date exists, otherwise the single aggregate row.
  let currentRow;
  if (di !== null) {
    const sorted = [...rows].sort((a, b) => {
      if (!a.date || !b.date) return 0;
      return a.date - b.date;
    });
    currentRow = sorted[sorted.length - 1];
  } else {
    currentRow = rows[0];
  }

  // Build sparkline history array: all rows sorted by date ascending.
  const history = (hi !== null)
    ? [...rows]
        .sort((a, b) => {
          if (!a.date || !b.date) return 0;
          return a.date - b.date;
        })
        .map(r => ({ date: r.date, val: r.history }))
        .filter(p => !Number.isNaN(p.val))
    : [];

  return {
    value:         currentRow.value,
    valueField:    valueField,
    label:         currentRow.label,
    comparison:    currentRow.comparison,
    history:       history,
    hasDate:       di !== null,
    hasComparison: ci !== null,
    hasHistory:    hi !== null,
  };
}

// ─── Card rendering ──────────────────────────────────────────────────────────
function renderCard(data, mode) {
  const card = document.getElementById('kpi-card');
  const body = document.getElementById('card-body');

  // Reset trend background classes.
  card.classList.remove('trend-up', 'trend-down');

  const displayLabel = data.label ?? data.valueField ?? 'KPI';

  switch (mode) {
    case 'comparison': {
      if (!data.hasComparison || data.comparison === null) {
        body.innerHTML = buildSimpleHTML(displayLabel, data.value) +
          '<p class="kpi-hint">Add a field to the Comparison encoding slot to enable this mode.</p>';
      } else {
        body.innerHTML = buildComparisonHTML(displayLabel, data.value, data.comparison);
      }
      break;
    }
    case 'trend': {
      if (!data.hasComparison || data.comparison === null) {
        body.innerHTML = buildSimpleHTML(displayLabel, data.value) +
          '<p class="kpi-hint">Add a field to the Comparison encoding slot to enable Trend mode.</p>';
      } else {
        const up = data.value >= data.comparison;
        card.classList.add(up ? 'trend-up' : 'trend-down');
        body.innerHTML = buildTrendHTML(displayLabel, data.value, up);
      }
      break;
    }
    case 'sparkline': {
      if (!data.hasHistory || data.history.length === 0) {
        body.innerHTML = buildSimpleHTML(displayLabel, data.value) +
          '<p class="kpi-hint">Add fields to the History and Date encoding slots to enable Sparkline mode.</p>';
      } else {
        body.innerHTML = buildSparklineHTML(displayLabel, data.value, data.history);
      }
      break;
    }
    case 'simple':
    default: {
      body.innerHTML = buildSimpleHTML(displayLabel, data.value);
      break;
    }
  }
}

// ─── Mode HTML builders ───────────────────────────────────────────────────────

function buildSimpleHTML(label, value) {
  return `
    <p class="kpi-label">${escHtml(label)}</p>
    <p class="kpi-value">${fmtBig(value)}</p>
  `;
}

function buildComparisonHTML(label, value, comparison) {
  const delta = calcDelta(value, comparison);
  const deltaStr = delta === null ? 'N/A' : fmtDelta(delta);
  const deltaClass = delta === null ? 'neutral' : delta >= 0 ? 'positive' : 'negative';
  return `
    <p class="kpi-label">${escHtml(label)}</p>
    <p class="kpi-value">${fmtBig(value)}</p>
    <div class="kpi-comparison-row">
      <span class="kpi-prev-value">vs ${fmtBig(comparison)}</span>
      <span class="kpi-delta ${deltaClass}">${deltaStr}</span>
    </div>
  `;
}

function buildTrendHTML(label, value, up) {
  const arrow = up ? '▲' : '▼';
  const arrowClass = up ? 'up' : 'down';
  return `
    <p class="kpi-label">${escHtml(label)}</p>
    <p class="kpi-value">${fmtBig(value)}</p>
    <p class="kpi-trend-arrow ${arrowClass}">${arrow}</p>
  `;
}

function buildSparklineHTML(label, value, history) {
  const W = 120, H = 40, PAD = 2;
  const vals = history.map(p => p.val);
  const minV = d3.min(vals);
  const maxV = d3.max(vals);
  const xScale = d3.scaleLinear().domain([0, history.length - 1]).range([PAD, W - PAD]);
  const yScale = d3.scaleLinear()
    .domain([minV === maxV ? minV - 1 : minV, minV === maxV ? maxV + 1 : maxV])
    .range([H - PAD, PAD]);

  const lineGen = d3.line()
    .x((_, i) => xScale(i))
    .y(p => yScale(p.val))
    .curve(d3.curveCatmullRom.alpha(0.5));

  const pathD = lineGen(history);

  const svgMarkup = `<svg width="${W}" height="${H}" viewBox="0 0 ${W} ${H}" xmlns="http://www.w3.org/2000/svg">
    <path d="${pathD}" fill="none" stroke="${COLOR_NEUTRAL}" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
  </svg>`;

  return `
    <p class="kpi-label">${escHtml(label)}</p>
    <p class="kpi-value">${fmtBig(value)}</p>
    <div class="kpi-sparkline">${svgMarkup}</div>
  `;
}

// ─── Utilities ────────────────────────────────────────────────────────────────

// Returns the relative delta as a fraction (e.g. 0.143 for +14.3%), or null if comparison is 0.
function calcDelta(value, comparison) {
  if (comparison === 0) return null;
  return (value - comparison) / Math.abs(comparison);
}

function escHtml(str) {
  return String(str)
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;');
}

function setError(msg) {
  const el = document.getElementById('error');
  if (el) el.textContent = msg;
}

function clearError() {
  const el = document.getElementById('error');
  if (el) el.textContent = '';
}
```

- [ ] **Step 3.2: Commit**

```bash
git add extensions/kpi-card/chart.js
git commit -m "feat: add D3 kpi-card rendering logic (4 display modes)"
```

---

## Task 4: Standalone Test Page (`test.html`)

The `test.html` mocks the full Tableau Extensions API with realistic revenue KPI data:
- Current period revenue: **1,200,000** (1.2M)
- Previous period revenue: **1,050,000** (1.05M)
- 12-month history array for the sparkline

**Files:**
- Create: `extensions/kpi-card/test.html`

- [x] **Step 4.1: Write test.html**

Create `extensions/kpi-card/test.html`:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>KPI Card — Browser Test</title>
  <script src="https://cdn.jsdelivr.net/npm/d3@7"></script>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      background: #eef0f3;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-start;
      padding: 24px 16px;
    }
    h2 {
      font-size: 13px;
      color: #7a828e;
      margin-bottom: 24px;
      font-weight: 500;
    }

    /* ── Copied from index.html to keep test standalone ─────────────────── */
    #kpi-card {
      position: relative;
      background: #ffffff;
      border-radius: 12px;
      box-shadow: 0 2px 12px rgba(0, 0, 0, 0.10);
      padding: 32px 36px 28px;
      min-width: 240px;
      max-width: 420px;
      width: 90vw;
      text-align: center;
    }
    #mode-buttons {
      position: absolute;
      top: 12px;
      right: 12px;
      display: flex;
      gap: 4px;
    }
    .mode-btn {
      font-size: 10px;
      font-weight: 600;
      letter-spacing: 0.03em;
      padding: 3px 8px;
      border-radius: 20px;
      border: 1.5px solid #c9cdd4;
      background: transparent;
      color: #7a828e;
      cursor: pointer;
      transition: background 0.15s, color 0.15s, border-color 0.15s;
      line-height: 1.5;
    }
    .mode-btn:hover { border-color: #3498db; color: #3498db; }
    .mode-btn.active { background: #3498db; border-color: #3498db; color: #ffffff; }
    .kpi-label {
      font-size: 12px;
      font-weight: 600;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: #9198a1;
      margin-bottom: 10px;
    }
    .kpi-value {
      font-size: 48px;
      font-weight: 700;
      line-height: 1.1;
      color: #1a2130;
      letter-spacing: -0.02em;
    }
    .kpi-comparison-row {
      margin-top: 10px;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      font-size: 14px;
      color: #555e6a;
    }
    .kpi-prev-value { font-weight: 500; }
    .kpi-delta { font-weight: 700; font-size: 15px; }
    .kpi-delta.positive { color: #27ae60; }
    .kpi-delta.negative { color: #e74c3c; }
    .kpi-delta.neutral  { color: #3498db; }
    #kpi-card.trend-up   { background: linear-gradient(135deg, #eafaf1 0%, #d5f5e3 100%); }
    #kpi-card.trend-down { background: linear-gradient(135deg, #fef9f9 0%, #fde8e8 100%); }
    .kpi-trend-arrow { font-size: 28px; margin-top: 6px; }
    .kpi-trend-arrow.up   { color: #27ae60; }
    .kpi-trend-arrow.down { color: #e74c3c; }
    .kpi-sparkline { margin-top: 16px; display: flex; justify-content: center; }
    .kpi-sparkline svg { display: block; }
    .kpi-hint { margin-top: 10px; font-size: 11px; color: #b0b8c1; font-style: italic; }
    #error { display: none; }
    #error:not(:empty) {
      display: block;
      margin-top: 12px;
      background: #fff0f0;
      color: #c0392b;
      border: 1px solid #f5c6c6;
      border-radius: 6px;
      padding: 8px 16px;
      font-size: 12px;
      max-width: 420px;
      text-align: center;
    }
  </style>
</head>
<body>
  <h2>KPI Card — Browser Test (no Tableau required)</h2>

  <div id="kpi-card">
    <div id="mode-buttons">
      <button class="mode-btn active" data-mode="simple">Simple</button>
      <button class="mode-btn" data-mode="comparison">Comparison</button>
      <button class="mode-btn" data-mode="trend">Trend</button>
      <button class="mode-btn" data-mode="sparkline">Sparkline</button>
    </div>
    <div id="card-body"></div>
  </div>
  <div id="error"></div>

  <script>
  // ─── Mock Tableau Extensions API ────────────────────────────────────────────
  // Data: 12-month revenue history, current = Dec (1,200,000), prev = Dec prior year (1,050,000)
  (function buildMock() {
    // Monthly revenue figures — 12 months of current year (Jan–Dec)
    const currentYearRevenue = [
      880000, 910000, 945000, 975000, 1020000, 990000,
      1050000, 1080000, 1100000, 1130000, 1165000, 1200000,
    ];

    // Corresponding comparison (previous-year same-month) values
    const prevYearRevenue = [
      760000, 790000, 820000, 840000, 870000, 855000,
      895000, 920000, 940000, 970000, 1010000, 1050000,
    ];

    const rows = currentYearRevenue.map((rev, i) => {
      const d = new Date(2024, i, 1);
      const dateStr = d.toISOString().split('T')[0];
      return [
        { value: 'Total Revenue' },      // label col 0
        { value: String(rev) },          // value col 1
        { value: String(prevYearRevenue[i]) }, // comparison col 2
        { value: String(rev) },          // history col 3 (same measure as value here)
        { value: dateStr },              // date col 4
      ];
    });

    window.tableau = {
      TableauEventType: { SummaryDataChanged: 'summary-data-changed' },
      extensions: {
        initializeAsync: async () => {},
        worksheetContent: {
          worksheet: {
            addEventListener: () => {},
            getVisualSpecificationAsync: async () => ({
              marksSpecificationCollection: [{
                encodingCollection: [
                  { id: 'value',      fieldCollection: [{ fieldName: 'Revenue' }] },
                  { id: 'label',      fieldCollection: [{ fieldName: 'Metric Name' }] },
                  { id: 'comparison', fieldCollection: [{ fieldName: 'Prev Revenue' }] },
                  { id: 'history',    fieldCollection: [{ fieldName: 'Revenue History' }] },
                  { id: 'date',       fieldCollection: [{ fieldName: 'Month' }] },
                ],
              }],
            }),
            getSummaryDataReaderAsync: async () => ({
              getAllPagesAsync: async () => ({
                columns: [
                  { fieldName: 'Metric Name',      index: 0, dataType: 'string' },
                  { fieldName: 'Revenue',          index: 1, dataType: 'float'  },
                  { fieldName: 'Prev Revenue',     index: 2, dataType: 'float'  },
                  { fieldName: 'Revenue History',  index: 3, dataType: 'float'  },
                  { fieldName: 'Month',            index: 4, dataType: 'date'   },
                ],
                data: rows,
              }),
              releaseAsync: async () => {},
            }),
          },
        },
      },
    };
  }());
  </script>

  <!-- Load chart logic after mock is defined -->
  <script src="chart.js"></script>
</body>
</html>
```

- [x] **Step 4.2: Open test.html in a browser and verify each mode**

Run a local server from the repo root:
```bash
python3 -m http.server 8080
```

Open: `http://localhost:8080/extensions/kpi-card/test.html`

Expected results per mode:
- **Simple:** Label "TOTAL REVENUE", value "1.2M"
- **Comparison:** Label, "1.2M", row showing "vs 1.1M" and a green delta "+14.3%"
- **Trend:** Green background card with "1.2M" and "▲" arrow
- **Sparkline:** "1.2M" with a 120×40 blue mini line chart below showing 12-month growth curve

Switching between mode buttons should update the card immediately with no page reload.

- [x] **Step 4.3: Commit**

```bash
git add extensions/kpi-card/test.html
git commit -m "feat: add browser test page for kpi-card with mock revenue data"
```

---

## Task 5: README.md

**Files:**
- Create: `extensions/kpi-card/README.md`

- [ ] **Step 5.1: Write README.md**

Create `extensions/kpi-card/README.md`:

```markdown
# KPI Card — Tableau Viz Extension

A Tableau Viz Extension that renders a clean KPI metric card with four selectable display modes.

**Production URL:** `https://vj-cyntexa.github.io/tableau-viz/extensions/kpi-card/index.html`

---

## Display Modes

| Mode | Description |
|---|---|
| Simple | Large primary value with label |
| Comparison | Current value vs previous/target with delta % |
| Trend | Value + up/down arrow + green/red background |
| Sparkline | Value + mini line chart (D3) from history data |

Switch modes with the pill buttons in the top-right corner of the card. The selected mode persists across data refreshes.

---

## Encoding Slots

| Slot | Type | Required | Description |
|---|---|---|---|
| `value` | Continuous measure | Yes | Primary KPI value displayed large |
| `label` | Discrete dimension | No | Card title/label; falls back to field name if absent |
| `comparison` | Continuous measure | No | Secondary value for delta % (Comparison and Trend modes) |
| `history` | Continuous measure | No | Measure values for the sparkline series (Sparkline mode) |
| `date` | Discrete dimension | No | Date dimension for sparkline X axis; also used to identify the most-recent row |

If an optional encoding is absent and a mode that requires it is selected, the card falls back to Simple mode with an inline hint.

---

## Tech Stack

- **D3.js v7** — sparkline rendering and number formatting
- **Tableau Extensions API 2.x** — encoding slot access and live data
- **Vanilla JS / HTML / CSS** — no build step

---

## Dev Workflow

1. Serve the repo root locally:
   ```bash
   python3 -m http.server 8080
   ```
2. Open the test page in your browser (no Tableau required):
   ```
   http://localhost:8080/extensions/kpi-card/test.html
   ```
3. To load inside Tableau Desktop:
   - Marks card → **Viz Extensions** → **Access Local Extension**
   - Select `extensions/kpi-card/kpi-card-local.trex`

---

## Adding to Tableau

1. Drag a sheet into a dashboard or open a worksheet.
2. On the Marks card choose **Viz Extension**.
3. Click **Add an Extension** → choose the `.trex` file:
   - Local dev: `kpi-card-local.trex` (requires local server on port 8080)
   - Production: `kpi-card.trex` (served from GitHub Pages)
4. Drag fields into the encoding slots:
   - At minimum, drag a numeric measure onto **Value**.
   - Optionally drag a dimension onto **Label**, another measure onto **Comparison**, a measure onto **History**, and a date onto **Date**.

---

## Author

Cyntexa — vishwajeet@cyntexa.com
```

- [ ] **Step 5.2: Commit**

```bash
git add extensions/kpi-card/README.md
git commit -m "docs: add README for kpi-card viz extension"
```

---

## Task 6: Push All

- [ ] **Step 6.1: Pull and push to origin**

```bash
git pull --rebase origin master && git push origin master
```

- [ ] **Step 6.2: Confirm GitHub Pages URL**

```bash
gh api repos/vj-cyntexa/tableau-viz/pages \
  -H "Accept: application/vnd.github+json" \
  | python3 -c "import sys,json; p=json.load(sys.stdin); print(p.get('html_url',''))"
```

Expected output: `https://vj-cyntexa.github.io/tableau-viz`

- [ ] **Step 6.3: Verify test page on GitHub Pages**

After the Pages build completes (~60–90 s), open:

`https://vj-cyntexa.github.io/tableau-viz/extensions/kpi-card/test.html`

Expected: the KPI card renders with all four modes working. If you see a 404, wait a minute and refresh.

---

## Self-Review Checklist

- [ ] `.trex` manifests declare all 5 encoding slots with correct role types and min/max counts
- [ ] `chart.js` reads `currentMode` from module scope — mode survives `SummaryDataChanged` re-renders
- [ ] `chart.js` guards against missing optional encodings (graceful fallback to Simple + hint)
- [ ] `chart.js` guards against `comparison === 0` in delta calculation (returns `null` → displays "N/A")
- [ ] `chart.js` selects the most-recent row by date when multiple rows are present
- [ ] Sparkline SVG is 120×40, no axes, `curveCatmullRom` smooth line, `COLOR_NEUTRAL` stroke
- [ ] Trend mode applies `trend-up` / `trend-down` CSS classes to card root and removes them on mode switch
- [ ] Mode buttons show filled/active state for the selected mode
- [ ] `test.html` mocks all 5 encoding slots and all API methods used by `chart.js`
- [ ] Mock data: current = 1,200,000 (Dec), comparison = 1,050,000 (Dec prev year), 12-month history
- [ ] All commits created before the final push
- [ ] GitHub Pages URL in `kpi-card.trex` matches the actual Pages base URL
